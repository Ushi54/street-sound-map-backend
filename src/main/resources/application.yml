server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db.umsmzcsbtrdspjobfjjm.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: ${SUPABASE_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
#    properties:
#      hibernate:
#        dialect: org.hibernate.spatial.dialect.postgis.PostgisPG10Dialect
  flyway:
    enabled: true
    baseline-on-migrate: true # 既存DBに初回実行時、自動でベースライン印を付ける
    baseline-version: 1 # いまの状態を "1" として扱う
